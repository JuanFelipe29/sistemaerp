<p style="color: green"><%= notice %></p>

<%= render @family %>

<div class="btn-group">
  <%= link_to "Volver a familias", families_path, class: "btn btn-primary" %>
  
  <%= button_to "Eliminar esta familia", @family, method: :delete, data: { confirm: "¿Estás seguro de que quieres eliminar esta familia?" }, class: "btn btn-danger" %>
</div>
